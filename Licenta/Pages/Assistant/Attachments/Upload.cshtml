@page
@model Licenta.Pages.Assistant.Attachments.UploadModel
@{
    ViewData["Title"] = "Upload analysis";
}
<h2>Upload analysis</h2>

<form method="post" enctype="multipart/form-data">
    <div class="mb-3">
        <label asp-for="Input.PatientId" class="form-label"></label>
        <select asp-for="Input.PatientId" asp-items="Model.Patients" class="form-select"></select>
        <span asp-validation-for="Input.PatientId" class="text-danger"></span>
    </div>
    <div class="mb-3">
        <label asp-for="Input.Type" class="form-label"></label>
        <input asp-for="Input.Type" class="form-control" placeholder="e.g. Hemogramă, Radiografie, RMN" />
    </div>
    <div class="mb-3">
        <label asp-for="Input.Description" class="form-label"></label>
        <textarea asp-for="Input.Description" rows="3" class="form-control"></textarea>
    </div>
    <div class="mb-3">
        <label class="form-label">File</label>
        <input asp-for="Input.File" class="form-control" type="file" />
        <span asp-validation-for="Input.File" class="text-danger"></span>
    </div>

    <button class="btn btn-primary">Upload</button>
    <a asp-page="/Assistant/Attachments/Index" class="btn btn-secondary ms-2">Back</a>
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
