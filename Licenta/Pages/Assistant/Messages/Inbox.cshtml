@page
@model Licenta.Pages.Assistant.Messages.InboxModel
@{
    ViewData["Title"] = "Messages";
}

@section Styles {
    <link rel="stylesheet" href="~/css/assistant.css" />
}

<h2 class="mb-3">Messages</h2>

<div class="mb-3">
    <a class="btn btn-primary btn-sm" asp-page="/Assistant/Messages/Create">
        New message
    </a>
</div>

<table class="table table-hover">
    <thead>
        <tr>
            <th>From</th>
            <th>Subject</th>
            <th>Date</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var m in Model.Messages)
        {
            <tr>
                <td>@(m.Sender.FullName ?? m.Sender.UserName)</td>
                <td>
                    <a asp-page="/Assistant/Messages/Details" asp-route-id="@m.Id">
                        @m.Subject
                    </a>
                </td>
                <td>@m.SentAt.ToLocalTime().ToString("g")</td>
            </tr>
        }
    </tbody>
</table>
